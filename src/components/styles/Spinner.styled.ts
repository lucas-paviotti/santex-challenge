import styled from "styled-components";

export const SpinnerStyled = styled.img`
    width: 100px;
    height: auto;
    position: absolute;
    left: 50%;
    transform: translate(-50%, 0);
    top: 10rem;
`